node_modules
# Keep environment variables out of version control
.env

# A real YouTube account's cookies, in case one's dropped here directly
# instead of via YTDLP_COOKIES_B64 (see ytdlp.ts) — never committed either way.
cookies.txt

# In-progress Telegram login state (scripts/telegramLoginStep.ts) — holds a
# pre-auth session string between the send-code/submit-code/submit-password
# steps. Deleted automatically once login completes; ignored here in case a
# run is ever abandoned mid-flow.
.telegram-login-state.json

/src/generated/prisma
